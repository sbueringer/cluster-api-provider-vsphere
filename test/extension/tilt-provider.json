{
  "name": "capv-test-extension",
  "config": {
    "version": "v1.10.99",
    "image": "gcr.io/k8s-staging-capi-vsphere/test-extension",
    "live_reload_deps": [
      "api",
      "handlers",
      "third_party",
      "main.go"
    ],
    "label": "CAPV-test-extension",
    "comment": "Add the ExtensionConfig for this Runtime extension; given that the ExtensionConfig can be installed only when capi_controller are up and running, it is required to set a resource_deps to ensure proper install order.",
    "additional_resources": [
      "config/tilt/extensionconfig.yaml"
    ],
    "resource_deps": [
      "capi_controller"
    ]
  }
}
